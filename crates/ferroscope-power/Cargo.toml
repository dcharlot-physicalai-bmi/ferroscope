[package]
name = "ferroscope-power"
description = "Read real power off the machine: Linux RAPL and macOS powermetrics, with the counter wraparound and the domain double-counting handled, and a clear refusal when the interface exists but this process cannot read it. No dependencies."
version = "0.1.1"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
keywords = ["rapl", "power", "energy", "powermetrics", "measurement"]
categories = ["hardware-support", "science"]

[dependencies]
