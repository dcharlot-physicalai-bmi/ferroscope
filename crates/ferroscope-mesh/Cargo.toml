[package]
name = "ferroscope-mesh"
description = "Read STL, write glTF, and compute what a mesh weighs: volume, centroid and inertia tensor from the geometry itself. No dependencies."
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
keywords = ["stl", "gltf", "mesh", "robotics", "inertia"]
categories = ["science::robotics", "graphics", "parsing"]

[dependencies]
